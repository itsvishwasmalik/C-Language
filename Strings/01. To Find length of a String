#include <stdio.h>
#include <string.h>
#include <stdlib.h>
int main()
{
    char s[30];
    int i,count=0;
    fgets(s,sizeof(s),stdin);

    for(i=1;s[i]!='\0';i++)
    {
        count++;
    }

    printf("%d",count);

    return 0;
}
