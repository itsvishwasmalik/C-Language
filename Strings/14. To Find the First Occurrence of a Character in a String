#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main()
{
    char s[100],ch;
    int i,count1=0;
    
    fgets(s,sizeof(s),stdin);
    scanf("%c",&ch);

    for(i=0;s[i]!=ch;i++)
    {
        count1++;
    }

    printf("%d",count1+1);

    return 0;
}
