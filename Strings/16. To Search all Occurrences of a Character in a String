#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main()
{
    char s[100],ch;
    int i;
    
    fgets(s,sizeof(s),stdin);
    scanf("%c",&ch);

    for(i=0;s[i]!='\0';i++)
    {
        if(s[i]==ch)
        {
            printf("%d,",i+1);
        }
    }

    return 0;
}
