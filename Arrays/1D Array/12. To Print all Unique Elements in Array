#include <stdio.h>
int main()
{
    int a[100],n,i,j,count;
    scanf("%d",&n);

    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }

    for(i=0;i<n;i++)
    {
        count=0;
        for(j=0;j<n;j++)
        {
            if(a[i]==a[j])
            {
                count++;
            }
        }
        if(count==1)
        {
            printf("%d ",a[i]);
        }
    }
    return 0;
}
/*
#include <stdio.h>
int h[100];
int main()
{
    int a[100],n,i;
    scanf("%d",&n);

    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }

    for(i=0;i<n;i++)
    {
        h[a[i]]++;

    }

    for(i=0;i<n;i++)
    {
        if(h[i]==1)
        {
            printf("%d ",i);
        }
    }
    return 0;
}
*/
